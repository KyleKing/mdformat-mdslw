korean no spaces limitation
.
이것은 기술 문서입니다.여러 문장이 포함되어 있습니다.문장 사이에 공백이 없습니다.
.
이것은 기술 문서입니다.여러 문장이 포함되어 있습니다.문장 사이에 공백이 없습니다.
.
--slw-min-line=0
--slw-wrap=0

korean with spaces after punctuation wraps correctly
.
이것은 첫 번째 문장입니다. 이것은 두 번째 문장입니다. 이것은 세 번째 문장입니다.
.
이것은 첫 번째 문장입니다.
이것은 두 번째 문장입니다.
이것은 세 번째 문장입니다.
.
--slw-min-line=0
--slw-wrap=0

korean mixed with english technical documentation
.
HTTP 프로토콜은 GET 및 POST와 같은 메서드를 사용하여 통신합니다. 서버는 상태 코드를 반환합니다. API 문서는 [docs.example.com](https://docs.example.com)에서 확인할 수 있습니다. 요청 헤더에는 `Content-Type` 및 `Authorization` 필드가 포함됩니다. 이 아키텍처는 김 선생님이 설계했습니다!
.
HTTP 프로토콜은 GET 및 POST와 같은 메서드를 사용하여 통신합니다.
서버는 상태 코드를 반환합니다.
API 문서는 [docs.example.com](https://docs.example.com)에서 확인할 수 있습니다.
요청 헤더에는 `Content-Type` 및 `Authorization` 필드가 포함됩니다.
이 아키텍처는 김 선생님이 설계했습니다!
.
--slw-min-line=0
--slw-wrap=0

korean informal blog tutorial mixed english
.
안녕하세요! 오늘은 Python 프로그래밍을 배워보겠습니다. 먼저 `pip install requests`로 패키지를 설치하세요. 그런 다음 코드에서 라이브러리를 가져옵니다. 쉽죠? 박 박사님은 매일 연습할 것을 권장합니다. 더 많은 튜토리얼을 보려면 제 블로그 [python-ko.dev](https://python-ko.dev)를 방문하세요!
.
안녕하세요!
오늘은 Python 프로그래밍을 배워보겠습니다.
먼저 `pip install requests`로 패키지를 설치하세요.
그런 다음 코드에서 라이브러리를 가져옵니다.
쉽죠?
박 박사님은 매일 연습할 것을 권장합니다.
더 많은 튜토리얼을 보려면 제 블로그 [python-ko.dev](https://python-ko.dev)를 방문하세요!
.
--slw-min-line=0
--slw-wrap=0

korean code-switching with framework names
.
Django 프레임워크를 사용하면 웹 애플리케이션을 만들 수 있습니다. `get_or_create()` 함수는 튜플을 반환합니다. 미들웨어는 뷰에 도달하기 전에 요청을 처리합니다. 이 씨가 캐싱 레이어를 구현했습니다. 예제는 [django-ko.readthedocs.io](https://django-ko.readthedocs.io) 문서를 참조하세요!
.
Django 프레임워크를 사용하면 웹 애플리케이션을 만들 수 있습니다.
`get_or_create()` 함수는 튜플을 반환합니다.
미들웨어는 뷰에 도달하기 전에 요청을 처리합니다.
이 씨가 캐싱 레이어를 구현했습니다.
예제는 [django-ko.readthedocs.io](https://django-ko.readthedocs.io) 문서를 참조하세요!
.
--slw-min-line=0
--slw-wrap=0

korean academic formal with english terms
.
최 교수의 연구는 여러 소스의 데이터를 분석합니다. 결과는 유의미한 상관관계, 즉 직접적인 관계를 나타냅니다. 방법론에는 통계 분석, A/B 테스트 등이 포함되며 전문 소프트웨어를 사용합니다. 정 박사님이 원고를 검토했습니다. 이러한 결과를 재현할 수 있습니까? 네, 논문의 3.2절에 따르면 가능합니다!
.
최 교수의 연구는 여러 소스의 데이터를 분석합니다.
결과는 유의미한 상관관계, 즉 직접적인 관계를 나타냅니다.
방법론에는 통계 분석, A/B 테스트 등이 포함되며 전문 소프트웨어를 사용합니다.
정 박사님이 원고를 검토했습니다.
이러한 결과를 재현할 수 있습니까?
네, 논문의 3.2절에 따르면 가능합니다!
.
--slw-min-line=0
--slw-wrap=0

korean with inline code and api endpoints
.
API를 사용하려면 먼저 인증해야 합니다. 샘플 코드는 다음과 같습니다: `const token = "abc123"`. 엔드포인트 `/api/users`는 JSON 데이터를 반환합니다. 응답에는 처리할 수 있는 `id`, `name` 등의 필드가 포함됩니다. 다른 프레임워크에서도 작동합니까? 물론입니다! 선호도에 따라 `fetch()` 또는 `axios`를 사용해 보세요!
.
API를 사용하려면 먼저 인증해야 합니다.
샘플 코드는 다음과 같습니다: `const token = "abc123"`.
엔드포인트 `/api/users`는 JSON 데이터를 반환합니다.
응답에는 처리할 수 있는 `id`, `name` 등의 필드가 포함됩니다.
다른 프레임워크에서도 작동합니까?
물론입니다!
선호도에 따라 `fetch()` 또는 `axios`를 사용해 보세요!
.
--slw-min-line=0
--slw-wrap=0

korean soft wrap mode with short sentences
.
짧은 텍스트. 더 많은 텍스트. 끝.
.
짧은 텍스트. 더 많은 텍스트. 끝.
.

korean question and exclamation marks
.
이것은 무엇입니까? 이것은 테스트입니다. 훌륭합니다! 작동합니다. 확실합니까? 네, 확실합니다!
.
이것은 무엇입니까?
이것은 테스트입니다.
훌륭합니다!
작동합니다.
확실합니까?
네, 확실합니다!
.
--slw-min-line=0
--slw-wrap=0

korean predominantly no spaces shows limitation
.
이 예제는 문장 사이에 공백이 없을 때의 제한을 보여줍니다.마침표가 있지만 공백이 없으면 올바르게 줄 바꿈되지 않습니다.이것은 한국어 텍스트에서 흔히 발생하는 문제입니다.올바르게 작동하려면 마침표 뒤에 공백을 추가해야 합니다.
.
이 예제는 문장 사이에 공백이 없을 때의 제한을 보여줍니다.마침표가 있지만 공백이 없으면 올바르게 줄 바꿈되지 않습니다.이것은 한국어 텍스트에서 흔히 발생하는 문제입니다.올바르게 작동하려면 마침표 뒤에 공백을 추가해야 합니다.
.
--slw-min-line=0
--slw-wrap=0
