japanese no spaces limitation
.
これは技術文書です.複数の文が含まれています.文の間にスペースがありません.
.
これは技術文書です.複数の文が含まれています.文の間にスペースがありません.
.
--slw-min-line=0
--slw-wrap=0

japanese with spaces after punctuation wraps correctly
.
これは最初の文です. これは2番目の文です. これは3番目の文です.
.
これは最初の文です.
これは2番目の文です.
これは3番目の文です.
.
--slw-min-line=0
--slw-wrap=0

japanese mixed with english technical documentation
.
HTTPプロトコルはGETやPOSTなどのメソッドを使用して通信します. サーバーはステータスコードを返します. APIドキュメントは [docs.example.com](https://docs.example.com) で確認できます. リクエストヘッダーには `Content-Type` と `Authorization` フィールドが含まれます. このアーキテクチャは田中さんが設計しました!
.
HTTPプロトコルはGETやPOSTなどのメソッドを使用して通信します.
サーバーはステータスコードを返します.
APIドキュメントは [docs.example.com](https://docs.example.com) で確認できます.
リクエストヘッダーには `Content-Type` と `Authorization` フィールドが含まれます.
このアーキテクチャは田中さんが設計しました!
.
--slw-min-line=0
--slw-wrap=0

japanese informal blog tutorial mixed english
.
皆さん、こんにちは! 今日はPythonプログラミングを学びます. まず、`pip install requests` でパッケージをインストールします. 次に、コードでライブラリをインポートします. 簡単ですよね? 佐藤博士は毎日練習することを推奨しています. 詳しいチュートリアルは私のブログ [python-ja.dev](https://python-ja.dev) をご覧ください!
.
皆さん、こんにちは!
今日はPythonプログラミングを学びます.
まず、`pip install requests` でパッケージをインストールします.
次に、コードでライブラリをインポートします.
簡単ですよね?
佐藤博士は毎日練習することを推奨しています.
詳しいチュートリアルは私のブログ [python-ja.dev](https://python-ja.dev) をご覧ください!
.
--slw-min-line=0
--slw-wrap=0

japanese code-switching with framework names
.
Djangoフレームワークを使用してWebアプリケーションを作成できます. `get_or_create()` 関数はタプルを返します. ミドルウェアはビューに到達する前にリクエストを処理します. 鈴木さんがキャッシング層を実装しました. 例については [django-ja.readthedocs.io](https://django-ja.readthedocs.io) のドキュメントを参照してください!
.
Djangoフレームワークを使用してWebアプリケーションを作成できます.
`get_or_create()` 関数はタプルを返します.
ミドルウェアはビューに到達する前にリクエストを処理します.
鈴木さんがキャッシング層を実装しました.
例については [django-ja.readthedocs.io](https://django-ja.readthedocs.io) のドキュメントを参照してください!
.
--slw-min-line=0
--slw-wrap=0

japanese academic formal with english terms
.
山田教授による研究は複数のソースからのデータを分析しています. 結果は有意な相関関係、つまり直接的な関係を示しています. 方法論には統計分析、A/Bテストなどが含まれ、専門ソフトウェアを使用しています. 高橋博士が原稿をレビューしました. これらの発見は再現可能ですか? はい、論文のセクション3.2によると可能です!
.
山田教授による研究は複数のソースからのデータを分析しています.
結果は有意な相関関係、つまり直接的な関係を示しています.
方法論には統計分析、A/Bテストなどが含まれ、専門ソフトウェアを使用しています.
高橋博士が原稿をレビューしました.
これらの発見は再現可能ですか?
はい、論文のセクション3.2によると可能です!
.
--slw-min-line=0
--slw-wrap=0

japanese with inline code and api endpoints
.
APIを使用するには、まず認証が必要です. サンプルコードは次のとおりです：`const token = "abc123"`. エンドポイント `/api/users` はJSONデータを返します. レスポンスには処理可能な `id`、`name` などのフィールドが含まれます. 他のフレームワークでも動作しますか? もちろんです! お好みに応じて `fetch()` または `axios` をお試しください!
.
APIを使用するには、まず認証が必要です.
サンプルコードは次のとおりです：`const token = "abc123"`.
エンドポイント `/api/users` はJSONデータを返します.
レスポンスには処理可能な `id`、`name` などのフィールドが含まれます.
他のフレームワークでも動作しますか?
もちろんです!
お好みに応じて `fetch()` または `axios` をお試しください!
.
--slw-min-line=0
--slw-wrap=0

japanese soft wrap mode with short sentences
.
短いテキスト. もっとテキスト. 終わり.
.
短いテキスト. もっとテキスト. 終わり.
.

japanese question and exclamation marks
.
これは何ですか? これはテストです. 素晴らしい! 動作します. 本当ですか? はい、本当です!
.
これは何ですか?
これはテストです.
素晴らしい!
動作します.
本当ですか?
はい、本当です!
.
--slw-min-line=0
--slw-wrap=0

japanese predominantly no spaces shows limitation
.
この例は文の間にスペースがない場合の制限を示しています.句点はありますがスペースがないため正しく改行されません.これは日本語テキストでよくある問題です.正しく動作させるには句点の後にスペースを追加する必要があります.
.
この例は文の間にスペースがない場合の制限を示しています.句点はありますがスペースがないため正しく改行されません.これは日本語テキストでよくある問題です.正しく動作させるには句点の後にスペースを追加する必要があります.
.
--slw-min-line=0
--slw-wrap=0
