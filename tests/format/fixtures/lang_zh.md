chinese no spaces limitation
.
这是一个技术文档.它包含多个句子.句子之间没有空格.
.
这是一个技术文档.它包含多个句子.句子之间没有空格.
.
--slw-min-line=0
--slw-wrap=0

chinese with spaces after punctuation wraps correctly
.
这是第一句话. 这是第二句话. 这是第三句话.
.
这是第一句话.
这是第二句话.
这是第三句话.
.
--slw-min-line=0
--slw-wrap=0

chinese mixed with english technical documentation
.
HTTP协议使用GET和POST等方法进行通信. 服务器返回状态码. API文档可在 [docs.example.com](https://docs.example.com) 查看. 请求头包含 `Content-Type` 和 `Authorization` 字段. 这个架构由张先生设计!
.
HTTP协议使用GET和POST等方法进行通信.
服务器返回状态码.
API文档可在 [docs.example.com](https://docs.example.com) 查看.
请求头包含 `Content-Type` 和 `Authorization` 字段.
这个架构由张先生设计!
.
--slw-min-line=0
--slw-wrap=0

chinese informal blog tutorial mixed english
.
大家好! 今天我们学习Python编程. 首先，使用 `pip install requests` 安装包. 然后在代码中导入库. 很简单，对吧? 李博士建议每天练习. 访问我的博客 [python-zh.dev](https://python-zh.dev) 获取更多教程!
.
大家好!
今天我们学习Python编程.
首先，使用 `pip install requests` 安装包.
然后在代码中导入库.
很简单，对吧?
李博士建议每天练习.
访问我的博客 [python-zh.dev](https://python-zh.dev) 获取更多教程!
.
--slw-min-line=0
--slw-wrap=0

chinese code-switching with framework names
.
Django框架允许创建Web应用程序. 函数 `get_or_create()` 返回一个元组. 中间件在到达视图之前处理请求. 王女士实现了缓存层. 请查看 [django-zh.readthedocs.io](https://django-zh.readthedocs.io) 文档了解示例!
.
Django框架允许创建Web应用程序.
函数 `get_or_create()` 返回一个元组.
中间件在到达视图之前处理请求.
王女士实现了缓存层.
请查看 [django-zh.readthedocs.io](https://django-zh.readthedocs.io) 文档了解示例!
.
--slw-min-line=0
--slw-wrap=0

chinese academic formal with english terms
.
由赵教授进行的研究分析了多个来源的数据. 结果表明存在显著相关性，即直接关系. 方法包括统计分析、A/B测试等，使用专业软件. 陈博士审阅了手稿. 这些发现可以复现吗? 是的，根据文章第3.2节!
.
由赵教授进行的研究分析了多个来源的数据.
结果表明存在显著相关性，即直接关系.
方法包括统计分析、A/B测试等，使用专业软件.
陈博士审阅了手稿.
这些发现可以复现吗?
是的，根据文章第3.2节!
.
--slw-min-line=0
--slw-wrap=0

chinese with inline code and api endpoints
.
要使用API，您需要先进行身份验证. 示例代码如下：`const token = "abc123"`. 端点 `/api/users` 返回JSON数据. 响应包含 `id`、`name` 等字段供您处理. 这适用于其他框架吗? 当然! 根据您的偏好尝试 `fetch()` 或 `axios`!
.
要使用API，您需要先进行身份验证.
示例代码如下：`const token = "abc123"`.
端点 `/api/users` 返回JSON数据.
响应包含 `id`、`name` 等字段供您处理.
这适用于其他框架吗?
当然!
根据您的偏好尝试 `fetch()` 或 `axios`!
.
--slw-min-line=0
--slw-wrap=0

chinese soft wrap mode with short sentences
.
短文本. 更多文本. 结束.
.
短文本. 更多文本. 结束.
.

chinese question and exclamation marks
.
这是什么? 这是一个测试. 太棒了! 它能工作. 你确定吗? 是的，确定!
.
这是什么?
这是一个测试.
太棒了!
它能工作.
你确定吗?
是的，确定!
.
--slw-min-line=0
--slw-wrap=0

chinese predominantly no spaces shows limitation
.
这个例子展示了当句子之间没有空格时的限制.虽然有句号，但是没有空格就无法正确换行.这是中文文本的常见问题.需要在句号后添加空格才能正确工作.
.
这个例子展示了当句子之间没有空格时的限制.虽然有句号，但是没有空格就无法正确换行.这是中文文本的常见问题.需要在句号后添加空格才能正确工作.
.
--slw-min-line=0
--slw-wrap=0
